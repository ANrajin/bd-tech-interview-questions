# Questions related to REST API

- What is REST?
- Can you explain `GET`, `POST`, `PUT`, `PATCH` and `DELETE` verb in REST API?
- What is the difference between the `PUT` and `PATCH` verb in REST API?
